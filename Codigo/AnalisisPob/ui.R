# Header -----------
header <- dashboardHeader(
  
)

# Sidebar -----------
sidebar <- dashboardSidebar(
  
)

# Body --------
body <- dashboardBody(
  
)


# Run the UI
dashboardPage(
  header = header, 
  sidebar = sidebar, 
  body = body
) %>% shinyUI()